apiVersion: v1
kind: Service
metadata:
  name: parking-manager-frontend-service
spec:
  type: LoadBalancer
  loadBalancerIP: "34.73.176.212"
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: parking-manager